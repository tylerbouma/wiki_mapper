import wikipedia
import requests
import json
import datetime

# use the wikipedia library to easily interact with wikipedia pages

# each link will be stored in it's own class
class 

# lets start by statically setting our starter page to 'python'
start_page = wikipedia.page("Python")

# get all of the links on the start page
for link in start_page.links:
    # store each of the links in an object
    
    link.content
    
